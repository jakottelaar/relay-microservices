api:
  insecure: true
  dashboard: true

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    exposedByDefault: false
    network: relay-network
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true

log:
  level: DEBUG
